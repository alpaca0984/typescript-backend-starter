version: "3.9"

services:
  app:
    build:
      context: .
      target: development
    ports:
      - 3000:3000
    volumes:
      - .:/workspace:cached
      - node-modules:/workspace/node_modules

volumes:
  node-modules:
